/**
 * 
 *  @description = @Folder/Paths will be process parallely so in this case ,
 *  notification , revenue , user will process simultanesoly.
 */

export default function ComplexDashboard() {
    return (
        <h1>Complex dashboard page</h1>
    )
}